package org.example.formatter

import org.example.model.PriceTik

class PriceFormatter {
    fun format(prices: List<PriceTik>): String {
        var result = ""
        for (item in prices) {
//            result += "${item.id} \n"
//            result += "${item.symbol} \n"
//            result += "${item.name}\n________\n\n"

        }
        return result
    }
}